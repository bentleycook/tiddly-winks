# Runtime state — lives in ~/.tiddly-winks/, not here
*.log
.env

# macOS
.DS_Store

# Dolt database files (added by bd init)
.dolt/
*.db

# Local Claude Code state (per-workspace settings, scheduled-task locks)
.claude/

# Beads runtime locks
.beads/*.lock
.beads/*.pid.lock
